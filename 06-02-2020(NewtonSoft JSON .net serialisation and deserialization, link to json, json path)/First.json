//Parsing json object from text
string json = @"{
    CPU: 'Intel',
    Drives: [
        'DVD read/writer',
        '500 gigabyte hard drive'
        ]
        }";

        JObject o = JObject.Parse(json);

//Parsing JSON Array from text
string json = @"[
    'Small',
    'Medium',
    'Large'
    ]";

    JArray a = JArray.Parse(json);


//loading json from a file
using(StreamReader reader = File.OpenText(@"c:\person"))
{
    JObject o = (JObject)JToken.ReadFrom(new JsonTextReader(reader));
}